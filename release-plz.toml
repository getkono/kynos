[workspace]
changelog_update = true
git_only = true
git_release_enable = true
git_tag_enable = true
release_always = false

[[package]]
name = "kynos"
changelog_path = "crates/kynos/CHANGELOG.md"
git_tag_name = "kynos-v{{ version }}"
git_release_name = "Kynos v{{ version }}"
